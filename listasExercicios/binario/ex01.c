#include <stdlib.h>
#include <stdio.h>
#include <time.h>

int main(int argc, char *argv[]) {
    FILE *arq;
    int i, ret, qtd;
    int *value;
    srand(time(NULL));

    qtd = rand() % (100000 - 100 + 1) + 100;


    return 0;
}
